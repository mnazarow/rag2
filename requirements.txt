# ---- минимум: на этом всё уже работает ----
httpx>=0.27            # HTTP-клиент: Telegram, модели, обход сайтов
numpy>=1.26            # векторный индекс
openpyxl>=3.1          # прайс-листы .xlsx
python-docx>=1.1       # документы Word

# ---- рекомендуется ----
PyMuPDF>=1.24          # быстрый и качественный разбор PDF
aiogram>=3.30          # продакшен-фреймворк Telegram-бота
xlrd>=2.0              # старые прайсы .xls
extract-msg>=0.48      # письма Outlook .msg
trafilatura>=1.12      # содержимое веб-страниц без навигации
watchdog>=4.0          # мгновенная реакция на изменения в папке
psutil>=5.9            # точные метрики сервера
httpx[socks]>=0.27     # прокси socks5 для Telegram

# ---- локальные модели поиска (нужен GPU или терпение) ----
# sentence-transformers>=3.0   # BGE-M3, USER-bge-m3, переранжирование
# torch>=2.3

# ---- голос ----
# faster-whisper>=1.0          # распознавание речи
# silero не требует установки: подтягивается через torch.hub

# ---- локальная языковая модель ----
# vllm>=0.6                    # сервер модели на видеокартах
# huggingface_hub>=0.24        # загрузка весов

# ---- телефония ----
# websockets>=12               # события Asterisk
# pjsua2                       # режим «как SIP-аппарат», требует сборки

# ---- продакшен-инфраструктура ----
# qdrant-client>=1.11          # замена numpy-индекса при росте базы
# langfuse>=2.50               # трассировка обращений к моделям
